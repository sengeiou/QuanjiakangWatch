<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- backgound color -->
    <solid android:color = "@color/color_title_green" />
    <!--set corner  -->
    <corners android:radius="5dp"/>
    <!-- set border width and color -->
    <stroke android:width="1dp" android:color="@color/color_title_green"/>
        
</shape>